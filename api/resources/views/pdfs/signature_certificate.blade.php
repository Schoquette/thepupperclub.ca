<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    body { font-family: 'DejaVu Sans', sans-serif; color: #3B2F2A; padding: 48px; font-size: 12px; }

    .header { border-bottom: 2px solid #C9A24D; padding-bottom: 20px; margin-bottom: 32px; }
    .brand  { font-size: 18px; font-weight: bold; letter-spacing: 2px; color: #3B2F2A; text-transform: uppercase; }
    .sub    { font-size: 10px; color: #C8BFB6; margin-top: 2px; }

    h1 { font-size: 20px; font-weight: bold; color: #3B2F2A; margin-bottom: 24px; }

    .section { margin-bottom: 20px; }
    .section-title { font-size: 9px; text-transform: uppercase; letter-spacing: 1px; color: #C8BFB6; margin-bottom: 8px; }
    .field { display: flex; justify-content: space-between; padding: 6px 0; border-bottom: 1px solid #F6F3EE; }
    .field-label { color: #C8BFB6; }
    .field-value { font-weight: bold; text-align: right; }

    .signature-box {
      border: 1px solid #C8BFB6;
      border-radius: 8px;
      padding: 16px;
      margin-top: 24px;
      background: #F6F3EE;
    }
    .signature-box .label { font-size: 9px; text-transform: uppercase; letter-spacing: 1px; color: #C8BFB6; margin-bottom: 8px; }
    .signature-img { max-width: 300px; max-height: 100px; display: block; }

    .footer { margin-top: 40px; border-top: 1px solid #F6F3EE; padding-top: 16px; font-size: 9px; color: #C8BFB6; }

    .badge {
      display: inline-block;
      background: #22c55e;
      color: white;
      padding: 4px 10px;
      border-radius: 20px;
      font-size: 10px;
      font-weight: bold;
      margin-bottom: 20px;
    }
  </style>
</head>
<body>

  <div class="header">
    <div class="brand">🐾 The Pupper Club</div>
    <div class="sub">Premium Dog Walking &amp; Care Services</div>
  </div>

  <h1>Document Signing Certificate</h1>
  <div class="badge">✓ Signed</div>

  <div class="section">
    <div class="section-title">Document Details</div>
    <div class="field">
      <span class="field-label">Document</span>
      <span class="field-value">{{ $document->filename }}</span>
    </div>
    <div class="field">
      <span class="field-label">Client</span>
      <span class="field-value">{{ $document->user?->name ?? '—' }}</span>
    </div>
    <div class="field">
      <span class="field-label">Signature Requested</span>
      <span class="field-value">{{ $document->signature_requested_at?->setTimezone('America/Vancouver')->format('F j, Y \a\t g:i A T') ?? '—' }}</span>
    </div>
  </div>

  <div class="section">
    <div class="section-title">Signing Details</div>
    <div class="field">
      <span class="field-label">Signed By</span>
      <span class="field-value">{{ $signer_name }}</span>
    </div>
    <div class="field">
      <span class="field-label">Date &amp; Time</span>
      <span class="field-value">{{ $signed_at->setTimezone('America/Vancouver')->format('F j, Y \a\t g:i A T') }}</span>
    </div>
    <div class="field">
      <span class="field-label">IP Address</span>
      <span class="field-value">{{ $signer_ip }}</span>
    </div>
  </div>

  <div class="signature-box">
    <div class="label">Signature</div>
    @if($signature_png)
      <img class="signature-img" src="data:image/png;base64,{{ $signature_png }}" alt="Signature" />
    @else
      <p style="color: #C8BFB6; font-style: italic;">No signature image available.</p>
    @endif
  </div>

  <div class="footer">
    <p>This certificate was automatically generated by The Pupper Club client portal.</p>
    <p style="margin-top: 4px;">The signature and metadata above constitute a valid record of electronic consent.</p>
  </div>

</body>
</html>
